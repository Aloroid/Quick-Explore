return {
	
	guideState = require(script.guideState),
	stripProps = require(script.stripProps),
	mapGuideColor = require(script.mapGuideColor),
	unwrap = require(script.unwrap),
	quickInsert = require(script.quickInsert),
	quickRemove = require(script.quickRemove),
	
	Theme = require(script.Theme),
	Statify = require(script.Statify),
	
	
}